# Environment Variables Template
# 
# IMPORTANT: DO NOT USE THIS FILE DIRECTLY!
# 
# For development: Use dev.env (already configured with your dev credentials)
# For production: Edit prod.env with your production credentials
#
# Both dev.env and prod.env are in .gitignore and will not be committed

# Database Configuration
MYSQL_ROOT_PASSWORD=your_secure_root_password_here
MYSQL_PASSWORD=your_secure_user_password_here

# Stripe Configuration
# Get from: https://dashboard.stripe.com/test/apikeys (test) or /apikeys (live)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
# Get from: https://dashboard.stripe.com/test/products (create products with prices)
STRIPE_PRICE_ID_WEEK=price_your_week_promotion_price_id
STRIPE_PRICE_ID_MONTH=price_your_month_promotion_price_id

# Google OAuth Configuration
# Get from: https://console.cloud.google.com/apis/credentials
# IMPORTANT: Configure authorized origins and redirect URIs in Google Console
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Notes:
# 1. Never commit dev.env or prod.env to version control
# 2. Use strong passwords with mixed case, numbers, and special characters
# 3. For production, use secrets management systems instead of env files
# 4. Generate secure passwords: openssl rand -base64 32
# 5. Use different Google Client IDs for dev and production environments
